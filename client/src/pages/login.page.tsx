import { LoginFormWrapper } from "../components/domains/login/login-form-wrapper/login-form-wrapper.component";
import SingleForm from "../layout/single-form.layout";

function LoginPage() {
    return (
        <SingleForm>
            <LoginFormWrapper />
        </SingleForm>
    );
}

export default LoginPage;
